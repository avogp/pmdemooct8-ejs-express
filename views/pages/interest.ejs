<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Interests</title>
    <%- include('../partials/head'); %>
  </head>
  <body class="container">
    <header><%- include('../partials/header'); %></header>

    <main>
      <div class="row">
        <div class="col-sm-8">
          <div class="jumbotron">
            <% interests.forEach(function(thingy) { %>
            <li>
              <strong><%= thingy.interest %></strong>
            </li>
            <% }); %>
          </div>
        </div>

        <div class="col-sm-4">
          <div class="well"> 
          </div>
        </div>
      </div>
    </main>

    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>